print("Script Loaded: Version 1.1")

-- Debugging: Confirm script load
print("Script Loaded: Clean Debugging Test")

-- Explicitly define the banned users table
local bannedUsers = {
    343052486, -- Initial banned user
}

-- Print the current table contents
print("Banned Users Table (Initial):", table.concat(bannedUsers, ", "))

-- Update the table manually
bannedUsers = {987654321} -- New banned user ID
print("Banned Users Table (Updated):", table.concat(bannedUsers, ", "))

-- Get the local player's User ID
local userId = game:GetService("Players").LocalPlayer.UserId
print("Local Player User ID:", userId)

-- Check if the local player is banned
if table.find(bannedUsers, userId) then
    print("User is banned.")
else
    print("User is not banned.")
end


local bannedUsers = {
    343052486, -- Example User ID
}

-- Function to check if a user is banned
local function isUserBanned(userId)
    return table.find(bannedUsers, userId) ~= nil
end

-- Get the local player's User ID
local userId = game:GetService("Players").LocalPlayer.UserId

-- Debugging: Print User ID and Banned Users List
print("Local Player User ID:", userId)
print("Current Banned Users List:", table.concat(bannedUsers, ", "))

-- Check if the user is banned
if isUserBanned(userId) then
    game:GetService("Players").LocalPlayer:Kick("You were banned from Cylindrical Appeal from discord copied to your clipboard") 
    print("You are banned from using this script.")
else
    print("You are not banned. Enjoy using the script!")
end
